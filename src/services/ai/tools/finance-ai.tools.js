/**
 * AI Tools for Finance data
 */
const financeTools = {
    async getCompanyBillingSummary(companyId, month, user) {
        // TODO: Implement using FinanceTransaction and Client models
        return { message: 'Data source not available yet' };
    },
    async getCompanyProfitability(companyId, month, user) {
        // TODO: Implement profitability logic
        return { message: 'Data source not available yet' };
    }
};

module.exports = financeTools;
